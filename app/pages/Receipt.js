import React, { Component } from 'react';
import {View, Text} from 'react-native';

export default class Receipt extends Component{
    render() {
        return(
            <Text>Welcome to Receipt Page</Text>
        )
    }
}